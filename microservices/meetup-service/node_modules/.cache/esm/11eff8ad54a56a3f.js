_54a‍.x([["validateRequestParams",()=>validateRequestParams]]);async function validateRequestParams(response, validateParams) {
  for (let i = 0; i < validateParams.length; i++) {
    const { schema, value } = validateParams[i];
    const validateError = schema.validate(value).error;
    console.log(validateError, validateError.isValid);
    if (validateError) {
      throw response.status(400).json(validateError.message);
    }
  }
}
