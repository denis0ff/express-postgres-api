let jwt;_2c2‍.x([["generateToken",()=>generateToken]]);_2c2‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

       function generateToken({ username }) {
  return jwt.sign({ username }, 'secret_key', { expiresIn: '1d' });
}
